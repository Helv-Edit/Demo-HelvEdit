{% if page.handle == 'connexion' %}
  {% section 'page-login' %}
{% else %}
  <div class="page-wrapper">
    <h1>{{ page.title }}</h1>
    <div class="page-content">
      {{ page.content }}
    </div>
  </div>
{% endif %}
